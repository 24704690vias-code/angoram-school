# Local development — not committed to git
#
# Leave VITE_API_URL commented out in development.
# client.js falls back to '/api' which Vite proxies to localhost:8080.
#
# VITE_API_URL=http://localhost:8080/api