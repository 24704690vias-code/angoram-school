/**
 * studentService.js — feature-level service for students.
 */
export {
  getStudents, getStudent, createStudent, updateStudent, deleteStudent,
  updateStudentStatus, assignStudentToClass, getStudentStatusLog,
} from '../../../api/studentApi';
